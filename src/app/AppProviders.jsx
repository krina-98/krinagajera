/**
 * Single place to add app-wide context (MotionConfig, Router, Theme...).
 * Adding providers here keeps App.jsx untouched. Transparent until Phase 2.
 */
export function AppProviders({ children }) {
  return children
}
