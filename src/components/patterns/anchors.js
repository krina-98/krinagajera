/**
 * Every in-page target sits under a fixed header, so anchors need to stop short
 * of the top edge. Written once here rather than repeated on each section —
 * change the header height and this is the only line that follows it.
 */
export const anchorOffset = 'scroll-mt-28'
