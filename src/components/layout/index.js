/**
 * Public barrel for site chrome — the frame every page sits inside.
 * Import from here: `import { SiteHeader } from '@/components/layout'`.
 */
export { SiteHeader } from './SiteHeader'
export { SiteFooter } from './SiteFooter'
