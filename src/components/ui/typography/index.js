/**
 * Public barrel for the typography system.
 * Import from here: `import { Heading, Text } from '@/components/ui/typography'`
 * (or from `@/components/ui`, which re-exports these).
 *
 * Named re-exports only — keeps the layer tree-shakeable.
 */
export { Heading } from './Heading'
export { Text } from './Text'
export { Eyebrow } from './Eyebrow'
export { Code } from './Code'
