/**
 * Public barrel for layout primitives.
 * Import from here: `import { Container } from '@/components/ui/layout'`
 * (or from `@/components/ui`, which re-exports these).
 */
export { Container } from './Container'
export { Section } from './Section'
